(function (ng) {
    'use strict';
    ng.module('mApp').controller('ContactCtrl', ContactCtrl);

    ContactCtrl.$inject = ['$scope']; //

    function ContactCtrl ($scope) {
        //create anchor for base this
        var vm = this;

    };
})(angular)
